# 环境变量配置指南

## 📋 环境变量列表

| 环境变量名称 | 描述 | 值示例 |
|-------------|------|--------|
| `API_KEY` | 火山方舟API密钥（首选） | `4226d7c4-27c7-48f7-8254-3ce512187c61` |
| `API_URL` | 火山方舟API端点URL（首选） | `https://ark.cn-beijing.volces.com/api/v3/chat/completions` |
| `MODEL` | 使用的AI模型 | `deepseek-r1-250528` |
| `PORT` | 服务器端口（Vercel会自动设置，本地部署可自定义） | `3000` |
| `DEEPSEEK_API_KEY` | DeepSeek API密钥（备选，与API_KEY二选一） | `your_deepseek_api_key` |
| `DEEPSEEK_API_URL` | DeepSeek API URL（备选，与API_URL二选一） | `https://api.deepseek.com/v1/chat/completions` |

## 🔧 配置优先级

系统会按照以下优先级读取环境变量：
1. `DEEPSEEK_API_KEY` > `API_KEY`
2. `DEEPSEEK_API_URL` > `API_URL`
3. 自定义值 > 默认值

## 📱 不同部署平台配置方法

### 1. 本地部署

创建 `.env` 文件，添加以下内容：
```env
API_KEY=你的火山方舟API密钥
API_URL=https://ark.cn-beijing.volces.com/api/v3/chat/completions
MODEL=deepseek-r1-250528
PORT=3000
```

### 2. Vercel部署

在Vercel项目的 **Settings > Environment Variables** 中添加：
- 键：`API_KEY`，值：`你的火山方舟API密钥`
- 键：`API_URL`，值：`https://ark.cn-beijing.volces.com/api/v3/chat/completions`
- 键：`MODEL`，值：`deepseek-r1-250528`

### 3. Docker部署

在 `docker run` 命令中添加环境变量：
```bash
docker run -d -p 3000:3000 \
  -e API_KEY=你的火山方舟API密钥 \
  -e API_URL=https://ark.cn-beijing.volces.com/api/v3/chat/completions \
  -e MODEL=deepseek-r1-250528 \
  your-image-name
```

### 4. 腾讯云部署

在腾讯云控制台的对应服务中添加环境变量：
- **TKE**：在Deployment的YAML中添加`env`字段
- **轻量应用服务器**：创建或修改`.env`文件
- **SCF云函数**：在函数配置中添加环境变量

## 🎯 注意事项

1. **API密钥安全**：不要将API密钥提交到Git仓库，使用`.gitignore`忽略`.env`文件
2. **二选一配置**：只需要配置`API_KEY`或`DEEPSEEK_API_KEY`中的一个
3. **模型选择**：确保使用的模型名称与API支持的模型一致
4. **端口配置**：Vercel会自动分配端口，不需要手动设置PORT
5. **URL格式**：确保API_URL格式正确，不带多余的空格或引号

## 📋 完整配置示例

```env
# 火山方舟配置（推荐）
API_KEY=4226d7c4-27c7-48f7-8254-3ce512187c61
API_URL=https://ark.cn-beijing.volces.com/api/v3/chat/completions
MODEL=deepseek-r1-250528
PORT=3000

# 或 DeepSeek配置（备选）
# DEEPSEEK_API_KEY=your_deepseek_api_key
# DEEPSEEK_API_URL=https://api.deepseek.com/v1/chat/completions
# MODEL=deepseek-r1-250528
# PORT=3000
```